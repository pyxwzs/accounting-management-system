# 个人记账管理系统 — 核心代码说明

本文档概括项目关键入口、安全、业务扩展点及与《需求》的对应关系，便于答辩或交接时快速定位源码。

---

## 1. 应用入口与全局能力

主启动类开启组件扫描与定时任务：

```11:18:admin-vue-api/src/main/java/com/it/CaseJopApplication.java
@SpringBootApplication
@ServletComponentScan//扫描@WebFilter、@WebServlet等组件
@EnableScheduling
public class CaseJopApplication {

    public static void main(String[] args) {
        SpringApplication.run(CaseJopApplication.class, args);
    }
```

- `@ServletComponentScan`：注册如 `JwtFilter` 等 Servlet 组件。
- `@EnableScheduling`：预算每日邮件预警（`BudgetAlertScheduler`）、财经 RSS 定时同步（`FinanceRssScheduler`）等。

---

## 2. 认证与请求拦截（JWT + Shiro）

`JwtFilter` 从请求头读取 `Authorization`，通过 Shiro `SecurityUtils.getSubject().login(new JwtToken(token))` 完成认证；失败时返回 JSON 错误（与前端 Axios 配合）。

核心逻辑见 `admin-vue-api/src/main/java/com/it/filter/JwtFilter.java`。

---

## 3. 操作日志（需求 5：AOP）

在带 `@Operation` 注解的 Controller 方法上环绕增强：成功写 `operation_result=0`，异常写 `operation_result=1` 并截断失败原因；操作人从 JWT 解析账号后查库取用户名，无效则记为「未知用户」。

```40:75:admin-vue-api/src/main/java/com/it/config/LogAspect.java
    @Around("pointCut()")
    public Object recordLog(ProceedingJoinPoint pjp) throws Throwable {
        MethodSignature methodSignature = (MethodSignature) pjp.getSignature();
        Method method = methodSignature.getMethod();
        Operation annotation = method.getAnnotation(Operation.class);
        if (annotation == null || !CommonUtils.stringIsNotBlack(annotation.value())) {
            return pjp.proceed();
        }

        String operatorName = resolveOperatorName();

        Log systemLog = new Log();
        systemLog.setOperation(annotation.value());
        systemLog.setName(operatorName);
        systemLog.setCreate_time(DateUtil.getNowDateSS());
        try {
            Object result = pjp.proceed();
            systemLog.setOperation_result("0");
            logService.insert(systemLog);
            return result;
        } catch (Throwable e) {
            systemLog.setOperation_result("1");
            String msg = e.getMessage();
            // ...
            systemLog.setOperation_fail_reason(msg);
            logService.insert(systemLog);
            throw e;
        }
    }
```

---

## 4. 我的预算（需求 1）

- 实体与表：`Budget`、`sys_budget`（周期类型、预警比例、上次发信日期等）。
- 周期边界工具：`BudgetPeriodUtil` — 按月/季/年计算当前周期起止，`none` 表示不按周期。
- 业务聚合：`BudgetAnalysisHelper`、`BudgetServiceImpl`、`BudgetController`。
- 定时预警：`BudgetAlertScheduler`（每日 9:00 等逻辑，需邮箱配置）。

---

## 5. 收件箱（需求 2）

- 前端路由：`/inbox`，`el-tabs` 切换系统公告与意见反馈；`/notice`、`/leaveword` 重定向并带 `tab` 参数。
- 后端：`NoticeController`、`LeavewordController`；意见反馈按当前用户 `user_id` 精确查询。

---

## 6. 存钱计划与完成预测（需求 3）

`PlanForecastHelper` 在分页查出计划列表后，调用 `InfoService.sumIncomeExpenseBetween` 汇总近 **30 天**收支，计算日均结余，并与「剩余目标 ÷ 剩余天数」比较，填充预测文案与是否按期等扩展字段。

```23:36:admin-vue-api/src/main/java/com/it/service/PlanForecastHelper.java
    public void enrichPlans(List<Plan> plans, String userId) {
        if (plans == null || plans.isEmpty() || userId == null) {
            return;
        }
        LocalDate today = LocalDate.now();
        LocalDate winStart = today.minusDays(RECENT_DAYS - 1);
        IncomeExpenseSummary sum = infoService.sumIncomeExpenseBetween(userId, winStart, today);
        float netTotal = sum.getTotalIncome() - sum.getTotalExpense();
        float netDaily = netTotal / (float) RECENT_DAYS;
        boolean hasData = sum.getTotalIncome() > 0f || sum.getTotalExpense() > 0f;

        for (Plan p : plans) {
            enrichOne(p, today, netDaily, hasData);
        }
    }
```

`PlanController` 在返回列表前调用该 Helper。

---

## 7. 财经资讯 RSS 与收藏（需求 4）

- `FinanceRssSyncService`：HTTP 拉取 RSS XML，解析 `<item>`，按 `source_url` 去重后写入 `sys_article`。
- 配置项：`finance.rss.url`、`scheduled.enabled`、`cron` 等（见 `application.yml`）。
- 收藏：`ArticleFavorite` / `sys_article_favorite`，接口如 `POST /article/favorite/toggle`（以实际 Controller 为准）。

---

## 8. 前端要点

- 路由：`vue-elementplus/src/router/index.js`。
- 全局请求：Axios 实例与 `Authorization` 头；Pinia 持久化登录信息。
- 预算可视化：ECharts 饼图；计划列表：`el-progress` 展示存钱与时间进度。

---

## 9. 目录速查（后端）

| 路径 | 含义 |
|------|------|
| `com.it.controller` | REST 控制器 |
| `com.it.service` / `impl` | 业务与实现 |
| `com.it.mapper` | MyBatis-Plus Mapper |
| `com.it.entity` | 实体 |
| `com.it.config` | Shiro、AOP、MyBatis、跨域等 |
| `com.it.filter` | JWT 过滤器 |
| `com.it.schedule` | 定时任务 |
| `resources/mapper` | XML 映射（如有） |
